<div>

  <%# <li id="post-<%= post.id %> 
  
  <span class="image">
    <%= link_to image_tag(post.image.url), post if post.image? %>
  </span>

  <span class="timestamp">
    Posted <%= time_ago_in_words(post.created_at) %> ago.
    <% if current_user?(post.user) %>
      <%= link_to "投稿を削除", post, method: :delete,
                                    data: { confirm: "削除しますか?" } %>
    <% end %>
  </span>
</div> 