<div class="row post-cards">
  <% posts.each do |post| %>
    <div class="col-md-8 mx-auto card-index">
      <div class="card">
        <div class="card-header">
          <div class="post-user">
            <% if post.user.avatar? %>
              <%= link_to image_tag(post.user.avatar.to_s, size: '50x50', class: "user_avatar rounded-circle", alt: "avatar image"), post.user, class: "mr-2" %>
            <% else %>
              <%= link_to image_tag("no-avatar.png", size: '50x50', class: "user_avatar rounded-circle", alt: "avatar image"), post.user, class: "mr-2" %>
            <% end %>
            <%= link_to post.user.name, post.user %>
          </div>
          <div class="post-info">
            <small><%= time_ago_in_words(post.created_at) %>前</small>
          </div>
        </div>
        <%= link_to image_tag(post.image.to_s, class: "post_image", alt: "post image"), post %>
      
        <div class="card-footer">
          <span><%= "コメント#{post.comments.count}件" %></span>
          <span><%= "いいね！#{post.likes.count}件" %></span>
        </div>
      </div>
    </div>
  <% end %>
</div>
<%= paginate posts %>

