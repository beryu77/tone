<% provide(:title, '保存した写真一覧') %>

<div class="row">
  <div class="col-md-8">
    <h4><%= "保存した写真一覧" %></h4>
    <% if @favorites.any? %>
      <ul class="">
        <%= render @favorites %>
      </ul>
      <%= paginate @favorites %>
    <% end %>
  </div>
</div>
