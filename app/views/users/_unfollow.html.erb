<%= form_for(current_user, url: relationship_path(user),
             method: :delete, remote: true) do |f| %>
  <%= hidden_field_tag :user_id, user.id %>
  <%= f.submit "フォロー中", class: "btn btn-secondary btn-follow" %>
<% end %>